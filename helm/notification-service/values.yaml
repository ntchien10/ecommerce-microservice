replicaCount: 1

image:
  repository: chiennt101020/notification-service
  tag: "1.0"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8083

containerPort: 8083

env:
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "192.168.0.105:9092"
  EUREKA_CLIENT_ENABLED: "false"
